Описание:
Проект представляет собой веб-приложение для распознавания нотных листов и генерации аудиодорожек на основе музыкальной нотации. 
Разработан на ASP.NET Core MVC с клиент-серверной архитектурой.
Основные функции:
   - Распознавание нотных листов
   - Генерация аудио из распознанных нот 
Хранение данных:  
   - Используется СУБД PostgreSQL для сохранения пользователей, аудиодорожек и метаданных (через Entity Framework Core)
   - Файлы (нотные листы, аудио) хранятся локально в папке UploadFiles/AllAlbums
Пользователи и безопасность:  
   - Регистрация/авторизация через ASP.NET Identity с ролевой моделью
   - Пользователи могут сохранять и управлять своими аудиодорожками


🚀 Развёртывание ASP.NET сайта  

Этот проект — это веб-приложение, созданное с использованием ASP.NET и базы данных PostgreSQL. Оно предназначено для работы с клиентами через веб-интерфейс и взаимодействует с базой данных для хранения и обработки информации. Развёртывание включает в себя установку всех необходимых компонентов, настройку подключения к базе и подготовку к запуску.  

 🛠️ Настройка  
Для корректной работы приложения необходимо настроить соединение с базой данных. Откройте файл `appsettings.json` и укажите строку подключения, которая содержит информацию о сервере базы данных, имени базы, пользователе и пароле. Пример строки подключения:  
```json  
"ConnectionStrings": {  
  "DefaultConnection": "Host=POSTGRES_SERVER;Database=MyDB;Username=USERNAME;Password=PASSWORD;"  
}  
```  

После настройки соединения убедитесь, что база данных PostgreSQL установлена и доступна для соединения. Если используется Entity Framework Core, выполните команду для применения миграций и создания базы данных:  
```bash
dotnet ef migrations add Init  
dotnet ef database update  
```  

Эта команда применит все созданные ранее миграции, создаст необходимые таблицы и подготовит базу данных к использованию. Если при запуске возникают ошибки, проверьте правильность строки подключения и наличие доступа к базе.  

 📌 Дополнительные настройки  
Возможно, потребуется изменить конфигурацию сервера базы данных, например, разрешить удалённые подключения или настроить права доступа для пользователя. Это можно сделать через pgAdmin или командной строкой PostgreSQL. Также рекомендуется убедиться, что брандмауэр не блокирует порт PostgreSQL (обычно `5432`).  

 🔗 Полезные ресурсы  
- [Видеоролик] (https://vkvideo.ru/video620979705_456239126?list=ln-YYDJT3thsUGyQd80fM)
- [Ссылка на github проекта] (https://github.com/rawer470/NoteSait)
- [Документация по ASP.NET](https://learn.microsoft.com/ru-ru/aspnet/core/)  
- [Официальная документация PostgreSQL](https://www.postgresql.org/docs/)  

